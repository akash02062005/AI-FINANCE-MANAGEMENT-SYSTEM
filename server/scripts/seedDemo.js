/**
 * DEPRECATED — fake demo transactions are no longer seeded. Real users start
 * with an empty account and build their own data. Kept as a stub so any
 * dynamic import doesn't crash.
 */
export async function seedDemoData() { /* no-op */ }
export default seedDemoData;
